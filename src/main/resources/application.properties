# Application name
spring.application.name=SyncroAPI
server.port=8080

# PostgreSQL Database configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/syncrodb
spring.datasource.username=arvildey
spring.datasource.password=Arvil1998#
spring.datasource.driverClassName=org.postgresql.Driver

# Hibernate / JPA properties
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Enable H2 Console (Optional for dev)
# spring.h2.console.enabled=true

# Enable Spring Security debug logs
logging.level.org.springframework.security=DEBUG
